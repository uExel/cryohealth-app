# Copy to .env for local development.
# iOS simulator: localhost works. Android emulator: use 10.0.2.2. Physical device:
# use your machine's LAN IP (e.g. http://192.168.1.20:3000) so the phone can reach it.
EXPO_PUBLIC_API_URL=http://localhost:3000
